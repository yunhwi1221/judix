# 심사비서 디자인 가이드 v0.1

- 문서 버전: v0.1 (초안)
- 작성일: 2026-08-24
- 작성자: 유니
- 목적: "심사비서" 웹서비스(랜딩페이지 + 심사위원/지원자/운영기관 화면)에 적용할 비주얼 디자인 시스템 정의
- 참고자료: 사용자 제공 레퍼런스 이미지 2장 (① "Rises" 스타일 랜딩페이지, ② "SYNC_ENGINE" 스타일 다크 SaaS 랜딩페이지)
- 핵심 제약: **메인 컬러 #4D026A 고정**, 포인트 컬러는 노란색 계열, **주 사용자층(심사위원)의 연령대를 고려한 높은 가독성** 우선

> 이 문서는 실제 화면 목업(mockup)이 아니라 **디자인 토큰·레이아웃 패턴 정의서**입니다. 이후 실제 화면(로그인, 심사위원 대시보드, 지원자 결과 화면 등)을 만들 때 이 문서의 컬러/타이포그래피/컴포넌트 규칙을 그대로 적용하면 됩니다.

---

## 1. 레퍼런스 이미지 분석 요약

### 1-1. 참고 이미지 ① (Rises 스타일 — 밝은 톤 랜딩)
- 크림색(warm off-white) 배경 위에 굵고 큰 헤드라인 타이포
- 우측에 원·사각형·둥근 코너 블록을 겹친 유기적(organic) 도형 조합 — 딱딱하지 않고 부드러운 인상
- 알약(pill) 모양의 CTA 버튼 (흰 배경 + 테두리 + 텍스트)
- 하단에는 테두리가 있는 화이트 카드 2~3개가 나란히 배치되는 "전략/가치 제안" 카드 섹션

**채택할 요소**: 크림 배경 히어로, 유기적 도형 장식, 알약형 버튼, 테두리형 화이트 카드 그리드

### 1-2. 참고 이미지 ② (SYNC_ENGINE 스타일 — 다크 테크 SaaS)
- 상단: 연회색 배경 + 아이콘 + 숫자/텍스트로 구성된 신뢰 지표(trust bar) 3개
- 중단: 굵은 흑색 헤드라인 + 회색 서브텍스트, 그 아래 아이콘 + "●LIVE" 배지 + 제목(한글+영문 병기) + 설명으로 구성된 기능 카드 3개
- 진한 네이비/퍼플 톤의 풀블리드 밴드 안에 **모노스페이스 느낌의 큼직한 숫자**로 표현된 핵심 지표 4개 (예: 764ms, 99.9%, 1.2B, 14+)
- 하단: 중앙 정렬된 화이트 카드 — 체크 아이콘 + 헤드라인 + 설명 + 상태 배지("STATUS: ALL SYSTEMS GREEN")

**채택할 요소**: 신뢰지표 바, LIVE 배지가 있는 기능 카드, 다크 밴드 안 대형 통계 숫자(모노스페이스), 상태 확인 카드 + 상태 배지
**폰트 인상**: 본문/제목은 군더더기 없는 그로테스크 산세리프(타이트한 자간, 미디엄~볼드), 통계 숫자는 고정폭(모노스페이스) 폰트로 "정밀하게 측정된 데이터"라는 인상을 줌 → 아래 2장 타이포그래피에 그대로 반영

---

## 2. 컬러 시스템

### 2-1. 메인 컬러 (Primary — 고정값)
| 토큰 | HEX | 용도 |
|---|---|---|
| Primary-950 | `#2E0140` | 다크 밴드 배경, 헤더 최상단 바 (참고이미지② 다크 섹션 대응) |
| Primary-900 | `#4D026A` | **브랜드 메인 컬러.** 헤드라인 강조, 주요 버튼, 로고, 아이콘 배경 |
| Primary-700 | `#7A1C99` | 호버/보조 강조, 그라디언트 보조색 |
| Primary-100 | `#F1E4F7` | 카드 배경 틴트, 선택된 상태(selected) 배경, 뱃지 배경 |

### 2-2. 포인트 컬러 (Accent — 노란색 계열)
보색 이론상 `#4D026A`(자주빛 보라, hue≈283°)의 정반대는 연두빛 옐로우(hue≈103°)지만, 실제로는 **퍼플 + 골드(금색 옐로우)** 조합이 "심사·수상·공신력" 이미지에 훨씬 잘 어울리고 시각적으로도 안정적입니다 (시상식·인증서 배색과 동일한 원리). 그래서 톤을 낮춘 골드 계열로 제안합니다.

| 토큰 | HEX | 용도 |
|---|---|---|
| Accent-600 | `#B8850C` | 밝은 배경 위 텍스트/아이콘 (대비 확보용, WCAG AA 통과) |
| Accent-500 | `#F5B700` | **포인트 컬러 기본값.** CTA 보조 버튼, LIVE 배지, 강조 숫자, 아이콘 포인트 |
| Accent-300 | `#FBD65C` | 다크 배경(Primary-950) 위 강조 숫자·텍스트 (밝은 골드로 대비 확보) |
| Accent-100 | `#FDF0C9` | 배지/하이라이트 배경, 주의 문구 배경 |

> ⚠️ Accent-500(`#F5B700`)는 흰 배경 위 **아이콘/배지/버튼 배경**에는 적합하지만, 흰 배경 위 **작은 텍스트**로 쓰면 명도 대비가 낮아 잘 안 보입니다. 텍스트로 쓸 때는 반드시 Accent-600을 사용하세요.

### 2-3. 배경·중립 컬러
| 토큰 | HEX | 용도 |
|---|---|---|
| Cream-BG | `#F7F2E8` | 히어로/랜딩 섹션 배경 (참고이미지① 느낌) |
| White | `#FFFFFF` | 카드, 본문 섹션 배경 |
| Gray-50 | `#F4F2F5` | 신뢰지표 바 등 옅은 구분 섹션 배경 |
| Gray-200 | `#E4DEE8` | 테두리, 구분선 |
| Text-900 | `#231A2B` | 본문 기본 텍스트 (거의 검정에 보라 톤을 살짝 섞음) |
| Text-500 | `#6B6270` | 보조 설명 텍스트 |

### 2-4. 상태 컬러 (판정/신뢰도 표시용 — 심사비서 기능과 직결)
| 토큰 | HEX | 용도 |
|---|---|---|
| Success | `#1E8E5A` | 근거 충분/관련성 높음/이의제기 반영 |
| Warning | `#B8850C` (Accent-600 재사용) | 보완 필요/근거 부족 |
| Danger | `#C4324B` | 불일치·표절 의심·미근거 |
| Info | `#3B5BA5` | 참고용 안내, 시스템 메시지 |

**가독성 원칙**: 심사위원 다수가 중장년층임을 고려해, 본문 텍스트에는 Gray-200 이하로 연한 회색을 절대 사용하지 않습니다(Text-500이 최저 허용치). 모든 텍스트-배경 조합은 WCAG AA(4.5:1) 이상을 기본으로 하고, 강조 숫자·중요 경고는 AAA(7:1)를 지향합니다.

---

## 3. 타이포그래피

### 3-1. 폰트 패밀리
레퍼런스 ②(오른쪽 이미지)의 타이포 인상을 기준으로 하되, 서비스가 **한국어+영어 동시 지원**이므로 한글 글리프가 없는 영문 전용 폰트(Inter 등)는 그대로 쓸 수 없습니다. 대신 그 인상을 그대로 재현하는 한글 대응 폰트로 매핑합니다.

| 역할 | 폰트 | 대체(fallback) | 비고 |
|---|---|---|---|
| 제목/본문 (UI 전반) | **Pretendard** | Inter, -apple-system, sans-serif | 레퍼런스②의 타이트하고 깔끔한 그로테스크 산세리프 인상을 한글 환경에서 그대로 구현하는 사실상의 표준 폰트. 오픈소스 무료 |
| 강조 통계 숫자 (KPI, 심사 점수, 정확도 %) | **JetBrains Mono** | Roboto Mono, monospace | 레퍼런스② 다크 밴드의 "764ms / 99.9%" 같은 고정폭 숫자 인상 재현. 자릿수가 흔들리지 않아 표·점수 정렬에도 유리 |

### 3-2. 크기 스케일 (가독성 우선 — 웹 표준보다 한 단계 크게)
심사위원 화면은 PC 웹 중심이며 장시간 텍스트를 읽는 업무용 화면이므로, 일반적인 마케팅 랜딩보다 **본문 최소 크기를 크게** 잡습니다.

| 토큰 | 크기 | 굵기 | 용도 |
|---|---|---|---|
| Display | 40px / 1.2 | Bold(700) | 랜딩 히어로 헤드라인 |
| H1 | 30px / 1.3 | Bold(700) | 페이지 타이틀 |
| H2 | 24px / 1.3 | SemiBold(600) | 섹션 제목 (예: "5대 평가기준") |
| H3 | 19px / 1.4 | SemiBold(600) | 카드 제목, 탭 라벨 |
| Body-L | 17px / 1.6 | Regular(400) | 본문 기본값 (심사위원이 읽는 지원서 분석 텍스트) |
| Body-M | 15px / 1.6 | Regular(400) | 보조 설명, 표 내용 |
| Caption | 13px / 1.5 | Medium(500) | 배지, 타임스탬프, 라벨 — 남용 금지 |
| Stat-Number | 36~48px / 1.1 | Bold(700), JetBrains Mono | KPI·점수·퍼센트 강조 숫자 전용 |

**금지 사항**: 본문에 Light(300) 이하 굵기 사용 금지, Body-M보다 작은 크기를 본문에 사용 금지, 회색조 텍스트에 배경과 대비 3:1 미만 조합 사용 금지.

---

## 4. 레이아웃 패턴 (두 레퍼런스 결합)

### 4-1. 히어로 섹션 *(레퍼런스① 기반, 랜딩페이지용)*
- 배경: Cream-BG
- 좌측: Display 크기 헤드라인(Primary-900) + Body-L 설명 + 알약형 Primary 버튼("무료로 시작하기" 등)
- 우측: Primary-900/Primary-700/Accent-500 3색을 조합한 유기적 도형 장식(원+라운드 블록). 레퍼런스①의 초록 사각형 자리에 **Accent-500(골드)**를 배치해 브랜드 포인트를 살림

### 4-2. 신뢰 지표 바 *(레퍼런스② 기반)*
- 배경: Gray-50, 가로 3~4열
- 아이콘(Primary-100 배경 원형 칩) + Stat-Number 스타일 숫자(Text-900) + Caption 설명
- 예시 문구는 실제 검증된 수치만 사용 (PRD 8장 "신뢰도 관리" 원칙과 동일하게, 근거 없는 숫자는 노출 금지 — 출시 전에는 "베타 운영 중" 등으로 대체)

### 4-3. 기능 소개 카드 그리드 *(레퍼런스② 기반)*
- H2 헤드라인 + Body-L 서브텍스트
- 카드 3열: 아이콘(Accent-100 배경) + 상태 배지(●운영중 등, Accent-500) + H3 제목(한글+영문 병기 가능) + Body-M 설명
- 5장 핵심 기능(질문 비서, 경력 검증, 코멘트 초안 등)을 이 카드 포맷에 매핑해서 소개 가능

### 4-4. 다크 통계 밴드 *(레퍼런스② 기반)*
- 배경: Primary-950 풀블리드
- 4열 그리드, 각 열: Caption(Gray 톤) 라벨 + Stat-Number(Accent-300, JetBrains Mono) 큰 숫자
- 예: "평균 심사 소요시간", "심사위원 만족도", "평가 편차 감소율" 등 9장 KPI 항목과 연결

### 4-5. 화이트 카드 그리드 *(레퍼런스① 기반)*
- 배경 White, 카드에 Gray-200 테두리(1px), 라운드 16px
- 페르소나별 가치 제안(심사위원용/지원자용/운영기관용)을 2~3개 카드로 배치할 때 사용
- 하나의 카드는 hover 시 Primary-700 테두리로 강조 가능 (레퍼런스① 중앙 카드의 파란 테두리 상태와 동일한 패턴)

### 4-6. 상태 확인 카드 *(레퍼런스② 기반 — 실제 제품 UI에 특히 유용)*
- 중앙 정렬 카드, 체크/경고 아이콘 + H3 헤드라인 + Body-M 설명 + 하단 상태 배지
- **실사용처**: 이의제기 처리 완료 화면, 심사 제출 완료 화면, "이 AI 분석은 참고용입니다" 안내 카드 등에 그대로 재사용

---

## 5. 컴포넌트 규칙

| 컴포넌트 | 규칙 |
|---|---|
| Primary 버튼 | 배경 Primary-900, 텍스트 White, 알약형(border-radius 999px), 높이 최소 48px (연령대 고려, 클릭 영역 확보) |
| Secondary 버튼 | 배경 White, 테두리 Primary-900 1.5px, 텍스트 Primary-900 |
| Accent 버튼(보조 CTA) | 배경 Accent-500, 텍스트 Text-900(검정에 가까운 색 — 흰 텍스트는 대비 부족) |
| 배지(Badge) | Caption 크기, 배경 Accent-100, 텍스트 Accent-600, 좌측에 상태 점(●) |
| 카드 | 라운드 16px, 그림자 최소화(0 2px 8px rgba(0,0,0,0.06)), 테두리 Gray-200 |
| 입력 필드 | 높이 최소 44px, 포커스 시 Primary-700 테두리 2px |
| 표(테이블) | 헤더 배경 Primary-900 + White 텍스트, 짝수행 Gray-50 배경으로 가독성 확보 |

---

## 6. 접근성·가독성 체크리스트 (심사위원 연령대 고려)

- 본문 최소 폰트 크기 15px 이상, 핵심 읽기 콘텐츠(지원서 분석)는 17px 기본
- 모든 텍스트-배경 대비 WCAG AA(4.5:1) 이상, 경고/위험 표시는 색상만이 아니라 아이콘+텍스트 라벨 병행 (색맹 고려)
- 버튼·클릭 영역 최소 44×44px
- 강조는 "색상+굵기" 조합으로, 색상 하나에만 의존하지 않음
- 한 화면에 Accent-500(골드) 남용 금지 — 페이지당 핵심 CTA/배지 1~2곳에만 사용해 "포인트"로서의 힘을 유지
- 애니메이션/트랜지션은 최소화(과도한 모션은 고령 사용자의 피로도를 높임)

---

## 7. 적용 우선순위 제안

1. 로그인/랜딩 화면 — 4-1, 4-2 패턴
2. 심사위원 대시보드(내가 평가할 팀 목록, 요약 카드) — 4-5, 5장 카드/표 규칙
3. 지원서 분석 상세 화면 — 3장 타이포그래피(Body-L) + 2-4 상태 컬러(강점/보완/위험 표시)
4. 운영기관 대시보드(KPI, 편차 시각화) — 4-4 다크 통계 밴드 패턴 재사용
5. 이의제기/제출 완료 등 확인 화면 — 4-6 상태 확인 카드

---

*다음 단계로 실제 화면 목업(와이어프레임)이 필요하면 별도로 요청해 주세요 — 이 문서를 기준 삼아 로그인 화면/심사위원 대시보드 등 구체적인 화면 디자인을 만들 수 있습니다.*
